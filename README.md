# Nkno.Dev

Me just learning React w/Material UI + Typescript against a C# Web API by polling data from a Magic the Gathering API hosted at https://docs.magicthegathering.io/.

I plugged in some DI stuff for using a local Cosmos DB, but I will probably be holding off on that.
